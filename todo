-- add the remaining operations to the parser;
-- add `get` function pointer to the Token struct (to be consistence with print function; maybe so we could even get rid of the assertions?);
-- handle division by zero;
-- handle errors in the arena.c;
-- support factors in front of variables and functions (e.g. 2x).
