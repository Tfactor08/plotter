-- support factors in front of variables and functions (e.g. 2x) ['factor' branch];
-- enhance the testing functionality;
-- `1-1` bug;
-- handle division by zero (done?).

In Question:
-- support removing allocated objects in arena.c ['arena' branch];
-- handle errors in arena.c;
-- get rid from arena?.

To be Mentioned in README:
-- recursive descent parsing and grammar;
-- polymorphic code in C: how and why it's useful;
-- the decision (so far) to give up on using arena allocators;
-- partial unity builds and Makefiles.
